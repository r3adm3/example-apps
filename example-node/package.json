{
  "dependencies": {
    //<-- it's safe to remove this dependency
    "express": "^4.5.1"
    //by deleting it in between the two comments-->
  },
  "name": "example-node",
  "version": "1.0.0",
  "description": "example node app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
